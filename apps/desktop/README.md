# Desktop component (planned)

Portable C++20 / Qt 6 Quick candidate. Owns presentation, interaction, GPU text
rendering, previews, accessibility, and carousel transitions. Receives session
snapshots and attention from the service. Sends input to an explicit session ID.

No implementation or toolkit dependency has been added yet. Validate rendering
and terminal-engine interoperability in a small spike before locking the stack.
